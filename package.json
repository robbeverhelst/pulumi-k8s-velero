{
  "name": "velero",
  "version": "1.0.0",
  "private": false,
  "description": "Pulumi Kubernetes stack: velero",
  "type": "module",
  "scripts": {
    "up": "pulumi up --yes --stack ${PULUMI_STACK:-prod}",
    "preview": "pulumi preview --stack ${PULUMI_STACK:-prod}",
    "destroy": "pulumi destroy --yes --stack ${PULUMI_STACK:-prod}",
    "refresh": "pulumi refresh --stack ${PULUMI_STACK:-prod}"
  },
  "dependencies": {
    "@pulumi/kubernetes": "^4.12.0",
    "@pulumi/pulumi": "^3.130.0",
    "yaml": "^2.8.1",
    "@homelab/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}